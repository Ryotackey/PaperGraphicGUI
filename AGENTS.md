# AGENTS.md

## プロジェクト概要

このプロジェクトは、Minecraft Paper向けのカスタム3D GUI APIを提供するプラグインです。

Display EntityおよびInteraction Entityを利用し、Minecraftのワールド空間上に自由度の高いGUIを表示することを目的とします。

最終的には、他のPaperプラグインから再利用できるGUIフレームワークとして提供することを目指します。

## 開発環境

- Minecraft: 26.2
- Paper: 26.2
- Java: 25
- ビルドシステム: Gradle Kotlin DSL

## 基本方針

- 非推奨のBukkit / Spigot APIよりPaper APIを優先する。
- 不要な外部依存関係を追加しない。
- 変更は小さく、目的を明確にする。
- 指示されていないコードを変更しない。
- 他のプラグイン開発者が利用することを前提として公開APIを設計する。
- 可能な限り公開APIと内部実装を分離する。
- 明示的に要求されない限り、古いMinecraft / Paperバージョンとの互換性は考慮しない。
- 将来の拡張を理由に、現在必要のない機能を先回りして実装しない。

## 作業境界

Codexは、Issueまたはユーザーから明示された作業範囲内のみ変更を行う。

### 自律的に行ってよいこと

以下は、要求された実装を完了するために必要な範囲で自律的に行ってよい。

- 既存コードの調査
- 関連するPaper APIの調査
- 実装方法の提案
- Issueの範囲内でのコード変更
- 必要なクラス・メソッド・テストの追加
- コンパイルエラーの修正
- フォーマットの修正
- テストの実行
- Gradleビルドの実行
- `git diff`、`git status` 等による変更内容の確認
- ユーザーが実装を承認したIssueについて、作業ブランチの作成、コミット、push、Pull Requestの作成

軽微な実装詳細については、既存設計とこのファイルの方針に従って判断してよい。

### 事前確認が必要なこと

以下を行う必要がある場合は、実装前に理由・選択肢・影響範囲を提示し、ユーザーの確認を得る。

- 公開APIの設計または大きな変更
- アーキテクチャの大きな変更
- 新しい外部ライブラリ・依存関係の追加
- Gradle構成の大きな変更
- JavaまたはPaperの対象バージョン変更
- Issueの範囲を超えるリファクタリング
- 複数の主要コンポーネントにまたがる設計変更
- 後方互換性を失う変更
- 永続化方式の導入または変更
- 非自明なパフォーマンス上のトレードオフを伴う変更

判断に迷う場合は、勝手に作業範囲を拡大せず確認する。

## 禁止事項

以下は、明示的な指示がない限り行わない。

### コード・設計

- Issueと無関係なコードを変更しない。
- 将来必要になるかもしれないという理由だけで機能を追加しない。
- 不要な抽象化、汎用化、デザインパターンを導入しない。
- 既存の公開APIを理由なく変更しない。
- deprecated APIを、新規コードで意図的に採用しない。
- Paper APIで実現できる処理のためにNMSへ直接依存しない。
- リフレクションを安易な互換性回避手段として使用しない。
- 根拠なくMinecraft/Paper固有の挙動を仮定しない。
- 警告や例外を、原因を確認せず握りつぶさない。

### ファイル・環境

- `.env`、秘密鍵、トークン、認証情報を読み取ったり変更したりしない。
- ビルド成果物や生成ファイルを手動編集しない。
- ユーザーのローカル設定を勝手に変更しない。
- 開発環境全体に影響するグローバルな設定変更を行わない。
- プロジェクト外のファイルを変更しない。

### Git / GitHub

- `main` に直接実装しない。
- ユーザーが実装を承認していないIssueや、承認されたIssueの範囲外の変更をコミットしない。
- ユーザーが実装を承認していないIssueや、検証が完了していない変更をpushしない。
- force pushしない。
- `git reset --hard` を使用しない。
- ユーザーが作成した未コミットの変更を破棄しない。
- ユーザーの変更を無断で上書きしない。
- 未マージのブランチを無断で削除しない。マージ済みの作業ブランチは、Git運用の規定に従って自動削除してよい。
- IssueやPull Requestを無断で閉じない。
- Pull Requestを無断でmergeしない。
- Git履歴を書き換えない。

### 破壊的操作

以下のような破壊的操作は、明示的な許可なしに実行しない。

- ファイルまたはディレクトリの大量削除
- データの削除
- ワールドデータの変更
- 本番Minecraftサーバーへの変更
- 本番環境へのデプロイ
- 外部サービスへの公開
- リモートリソースの削除（ただし、Git運用の規定に基づくマージ済み作業ブランチの削除を除く）

## 実装前

重要な変更を行う前に、以下を実施する。

1. `AGENTS.md` を確認する。
2. 関連するIssueまたは要求内容を確認する。
3. 既存コードを調査する。
4. 関連するPaper APIを確認する。
5. 実装方針を提示する。
6. 変更予定のファイルを提示する。

設計上の判断が必要な場合は、理由と選択肢を提示する。

不明な仕様を推測して大きな実装を進めない。

## 実装

- 1つのIssueに関係する変更だけを行う。
- クラスやメソッドには明確な責務を持たせる。
- Minecraft/Paper固有の処理と、GUIのドメインロジックを可能な範囲で分離する。
- GUIの状態をグローバルな可変状態として管理しない。
- プレイヤー固有のGUI状態はプレイヤーUUIDまたは専用のセッションオブジェクトによって管理する。
- Entityを生成した場合、そのライフサイクルと削除方法を明確にする。
- GUIの所有者以外からのInteractionイベントを受け付けない。
- コメントは「何をしているか」ではなく、コードだけでは分かりにくい「なぜそうしているか」を説明する場合に使用する。

## 公開API

このプロジェクトはGUIライブラリとして利用されることを想定する。

そのため公開APIでは以下を重視する。

- 利用側のコードがMinecraft内部のEntity管理を意識しなくてよい設計にする。
- 内部実装のクラスを不要に公開しない。
- API利用者がPaper Entityを直接管理する必要がないようにする。
- 破壊的変更につながる公開APIの追加は慎重に行う。
- 実装の都合をそのまま公開APIに露出させない。

## テスト

Minecraftサーバーを必要としないロジックについては、可能な限りユニットテストを追加する。

特に以下はMinecraft APIから分離してテスト可能にすることを検討する。

- GUI座標計算
- Transform計算
- レイアウト計算
- GUI状態管理
- コンポーネント階層

Minecraftサーバー上で実際に確認していない動作について、「動作確認済み」と報告しない。

## 検証

実装後は以下を実施する。

1. テストを実行する。
2. Gradleビルドを実行する。
3. エラーと警告を確認する。
4. `git diff` を確認する。
5. 変更したファイルを確認する。
6. 要求されていない変更が含まれていないことを確認する。

最後に以下を報告する。

- 実装内容
- 変更したファイル
- 実行したテスト
- ビルド結果
- 未確認事項
- 技術的な懸念点

JARなど、ユーザーが実環境で確認する成果物を生成した場合は、最終報告で成果物のパスまたはリンクを示し、続けてユーザーが確認する項目を具体的なチェックリストとして表示する。

チェックリストでは、Codexが自動検証した項目と、Minecraftサーバーなどの実環境でユーザーが手動確認する項目を明確に区別する。

## Git運用

- `main` ブランチへ直接実装しない。
- Issueまたはユーザーから承認された作業単位ごとに、`main` から `codex/<issue-summary>` 形式の作業ブランチを作成する。
- 1つのブランチでは原則として1つのIssueを扱う。
- コミットは目的ごとに小さくまとめる。
- ユーザーがIssueの実装を承認した後は、作業ブランチの作成、実装、テスト、ビルド、コミット、push、Pull Requestの作成までを追加のユーザー確認なしで進めてよい。
- Pull Requestは、実装と自動検証が完了してから作成する。
- Pull Requestには、対象Issueまたは要求、実装内容、変更ファイル、実行したテスト、ビルド結果、未確認事項、技術的な懸念点を記載する。
- JARなどを生成した場合は、Pull Requestにも成果物と実環境での手動確認項目を記載する。
- ユーザーの実環境テストで問題が見つかった場合は、同じ作業ブランチで修正し、同じPull Requestを更新する。
- Pull Requestのmergeはユーザーが行う。Codexは明示的な依頼なしにmergeしない。
- Pull Requestがマージ済みであることを確認したら、対応する作業ブランチは追加確認なしでローカルとリモートから自動削除してよい。未マージのブランチは削除しない。
- merge後に次のIssueへ進む場合は、更新された `main` から新しい作業ブランチを作成する。
- force pushを行わない。
- ユーザーが作成した変更を勝手に破棄しない。

## AIによる開発

Codexは実装を自動的に進めるだけではなく、設計・調査・レビューを支援する役割として使用する。

重要な設計判断については、実装前に以下を説明する。

- 何を解決するための設計なのか
- なぜその設計を選択するのか
- 他にどのような選択肢があるのか
- 将来どの部分が変更される可能性があるのか

大規模な変更を一度に行わず、レビュー可能な単位に分割する。
